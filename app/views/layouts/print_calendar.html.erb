<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "In Store Marketing" %></title>
    <style type="text/css">
    </style>
  </head>
  <body>
      <div class="container-fluid">
        <%= yield %>
      </div>
  </body>
</html>